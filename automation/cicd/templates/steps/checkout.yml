parameters:
  - name: repo
    type: string

  - name: branch
    type: string

steps:
  - checkout: pipeline

  - checkout: git://$(System.TeamProject)/${{ parameters.repo }}@${{ parameters.branch }}
    persistCredentials: true
    displayName: 'Checkout project: ${{ parameters.repo }}@${{ parameters.branch }}'
